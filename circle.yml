dependencies:
  override:
    - pyenv local 3.6.0
    - python -m pip install -U pip wheel tox tox-pyenv setuptools
    # - pyenv local 3.4.3 3.5.2 3.6.0
test:
  override:
    - tox -r -e py36-django-111
